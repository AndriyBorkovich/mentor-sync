namespace MentorSync.Recommendations.Features.CheckBookmark;

/// <summary>
/// Result model for checking if a mentor is bookmarked.
/// </summary>
public sealed record CheckBookmarkResult(bool IsBookmarked);
